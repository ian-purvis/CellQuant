name: cellquant-napari
channels:
  - conda-forge
dependencies:
  - python=3.11.15
  - pip
  - pip:
      # Editable CellQuant + test extras + Cellpose-SAM (v4). The Windows
      # installer builds a sibling v3 env with cellpose-v3 instead.
    - -e .[cellpose-v4,test]
